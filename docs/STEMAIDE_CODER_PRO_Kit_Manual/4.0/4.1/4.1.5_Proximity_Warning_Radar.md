# Project 4.5: Proximity Warning Radar

| **Description** Learners will engineer an advanced 'Proximity Warning Radar' multi-component system. This design integrates HC-SR04 Ultrasonic + Active Buzzer + Traffic Light Module into a cohesive industrial automation unit.|------------------|----------------------------------------------------------------|
| **Use case**     |This project connects to real-world industrial and IoT applications such as: Project 66 provides essential master-level engineering skills for real-world IoT, safety, and control systems.|

## Components (Things You will need)

| ![Arduino Uno](../../assets/coder_pro/components/arduino.webp) | ![Bread Board](../../assets/coder_pro/components/breadboard.webp) | ![Arduino USB Cable](../../assets/coder_pro/components/usbcable.webp) | ![Jumper Wires](../../assets/coder_pro/components/jumperwires.webp) |![Ultrasonic Sensor](../../assets/coder_pro/components/ultrasonic.webp) |![Buzzer](../../assets/coder_pro/components/Buzzer.png) |![Traffic Light Module](../../assets/coder_pro/components/trafficmodule.webp) |
|------------------------------------------------------ | ----------------------------------------------------------- | --------------------------------------------------------- | ------------------------------------------------------ | ------------------------------------------------------ |------------------------------------------------------ |------------------------------------------------------ |

## Building the circuit

Things Needed:

- Arduino Uno = 1
- Arduino USB cable = 1
- Servo motor = 1
- Jumper Wires
- Ultrasonic Sensor
- Buzzer
- Traffic Light Module

## Mounting the component on the breadboard and wiring the circuit

**Step 1:** Connect the sensor (input) leads to the correct **Analog/Digital pins** on the Arduino.

**Step 2:** Connect the actuator and display (output) leads to the required **PWM/I2C pins** on the Arduino.

**Step 3:** Connect all **VCC** power lines to the correct **5V or 3.3V pins** on the Arduino.

**Step 4:** Connect all **GND** lines to any available **GND pin** on the Arduino.

**Step 5:** Check all wiring connections to ensure VCC, 5V, 3.3V, and GND are correctly connected.

**Step 6:** Connect the Arduino **USB cable** to power the board and begin programming.

![Components on breadboard](../../assets/coder_pro/4.0/Proximity_Warning_Radar/circuit_1.png)

_Make sure to connect the Arduino USB cable to the Arduino board._

## PROGRAMMING

**Step 1:** Open your Arduino IDE. See how to set up here: [Getting Started](../../Getting Started/Arduino_IDE_Setup.md).

**Step 2:** Write the complete program implementing the system logic with appropriate pin definitions, setup configuration, and the main control loop.

```cpp
// Project 66: Proximity Warning Radar
// Target Integration: HC-SR04 Ultrasonic + Active Buzzer + Traffic Light Module

const int trigPin = 2;
const int echoPin = 3;
const int redLed = 4;
const int yellowLed = 5;
const int greenLed = 6;
const int buzzer = 7;

void setup() {
  Serial.begin(9600);
  pinMode(trigPin, OUTPUT);
  pinMode(echoPin, INPUT);
  pinMode(redLed, OUTPUT);
  pinMode(yellowLed, OUTPUT);
  pinMode(greenLed, OUTPUT);
  pinMode(buzzer, OUTPUT);
  Serial.println("Project 66: Proximity Warning Radar Initialized...");
}

void loop() {
  long duration, distance;
  
  // Trigger sensor
  digitalWrite(trigPin, LOW);
  delayMicroseconds(2);
  digitalWrite(trigPin, HIGH);
  delayMicroseconds(10);
  digitalWrite(trigPin, LOW);
  
  // Read echo
  duration = pulseIn(echoPin, HIGH);
  distance = (duration / 2) / 29.1;

  Serial.print("Distance: ");
  Serial.print(distance);
  Serial.println(" cm");

  // Logic control
  if (distance > 0 && distance < 10) {
    // Red Light + Buzzer

    digitalWrite(redLed, HIGH);
    digitalWrite(yellowLed, LOW);
    digitalWrite(greenLed, LOW);
    tone(buzzer, 1000); 
  } else if (distance >= 10 && distance < 25) {
    // Yellow Light
    digitalWrite(redLed, LOW);
    digitalWrite(yellowLed, HIGH);
    digitalWrite(greenLed, LOW);
    noTone(buzzer);
  } else {
    // Green Light
    digitalWrite(redLed, LOW);
    digitalWrite(yellowLed, LOW);
    digitalWrite(greenLed, HIGH);
    noTone(buzzer);
  }
  delay(200);
}

```

**Step 7:** Save your code. _See the [Getting Started](../../Getting Started/Arduino_IDE_Setup.md) section_

**Step 8:** Select the arduino board and port _See the [Getting Started](../../Getting Started/Arduino_IDE_Setup.md) section:Selecting Arduino Board Type and Uploading your code_.

**Step 9:** Upload your code. _See the [Getting Started](../../Getting Started/Arduino_IDE_Setup.md) section:Selecting Arduino Board Type and Uploading your code_

## CONCLUSION

By integrating the HC-SR04 sensor, active buzzer, and traffic light module, students gained practical experience in building sensor-based safety and automation systems.

This project strengthens understanding of input processing, actuator control, and real-world IoT applications, preparing learners to develop more advanced monitoring and control solutions.