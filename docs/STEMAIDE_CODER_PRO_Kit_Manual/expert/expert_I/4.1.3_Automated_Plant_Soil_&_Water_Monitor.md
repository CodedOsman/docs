# 4.1.3. Automated Plant Soil & Water Monitor

| **Description** | Students build real-world experience in sensor bus integration, actuation logic, and fault-tolerant system design.|
|------------------|---------------------------------------------------------------- |
| **Use case**     | This project connects to real-world industrial and IoT applications such as: Project 64 provides essential master-level engineering skills for real-world IoT, safety, and control systems. |

## Components (Things You will need)

| ![Arduino Uno](../../../assets/coder_pro/components/arduino.webp) | ![Arduino USB Cable](../../../assets/coder_pro/components/usbcable.webp) | ![Bread Board](../../../assets/coder_pro/components/breadboard.webp) | ![Jumper Wires](../../../assets/coder_pro/components/jumperwires.webp) | ![LCD Display](../../../assets/coder_pro/components/lcdscreen.jpg) | ![Buzzer](../../../assets/coder_pro/components/Buzzer.png) | ![Servo Motor](../../../assets/coder_pro/components/servomotor.webp) | ![Water Sensor](../../../assets/coder_pro/components/watersensor.jpg) |
| --- | --- | --- | --- | --- | --- | --- | --- |

## Building the circuit

Things Needed:

- Arduino Uno = 1
- Arduino USB cable = 1
- Servo motor = 1
- Jumper Wires
- Water Test Module
- LCD Display
- Buzzer
- Bread Board


## Mounting the component on the breadboard and wiring the circuit

**Step 1:** Connect the Power and Ground Rails

Connect the 5V pin on the Arduino Uno board to the positive (+) power rail on the breadboard using a jumper wire. Then, connect a GND pin on the Arduino Uno board to the negative (−) ground rail.

**Step 2:** Connect the Water Sensor

Connect the VCC pin of the water sensor to the 5V power rail and the GND pin to the ground rail using jumper wires. Then, connect the Signal (S) pin of the water sensor to Digital Pin 7 (D7) on the Arduino Uno board.

**Step 3:** Connect the Active Buzzer

Connect the positive (+) terminal of the active buzzer to Digital Pin 12 (D12) on the Arduino Uno board and the negative (−) terminal to the ground rail using jumper wires.

**Step 4:** Connect the I²C LCD Module

Connect the VCC pin of the I²C LCD module to the 5V power rail and the GND pin to the ground rail using jumper wires. Then, connect the SDA pin to A4 and the SCL pin to A5 on the Arduino Uno board.

**Step 5:** Perform a Final Check

Check all power, signal, ground, and polarity connections against the Circuit Connections table and wiring diagram. Ensure that all components share a common ground and that there are no loose or misplaced wires.

**Step 6:** Connect the USB Cable

After confirming that all connections are correct, connect the USB cable to the Arduino Uno board and the computer to power the circuit.

![Components on breadboard](../../../assets/coder_pro/expert/Automated_Plant_Soil_&_Water_Monitor/circuit_1.png)

_Make sure to connect the Arduino USB cable to the Arduino board._

## PROGRAMMING

**Step 1:** Open your Arduino IDE. See how to set up here: [Getting Started](../../Getting Started/Arduino_IDE_Setup.md).

**Step 2:** Write the complete program implementing the system logic with appropriate pin definitions, setup configuration, and the main control loop.

```cpp
#include <Wire.h>
#include <LiquidCrystal_I2C.h>

// LCD Address (Change to 0x3F if required)
LiquidCrystal_I2C lcd(0x27, 16, 2);

// Pin Definitions
const int waterSensorPin = 7;     // Digital output from Water Sensor
const int buzzerPin = 12;

// Variables
bool waterDetected = false;

void setup()
{
  pinMode(waterSensorPin, INPUT);
  pinMode(buzzerPin, OUTPUT);

  digitalWrite(buzzerPin, LOW);

  Serial.begin(9600);

  lcd.init();
  lcd.backlight();

  lcd.clear();
  lcd.setCursor(0,0);
  lcd.print("Plant Monitor");
  lcd.setCursor(0,1);
  lcd.print("Initializing");

  delay(2000);
  lcd.clear();

  Serial.println("Project 64 Started...");
}

void loop()
{
  waterDetected = digitalRead(waterSensorPin);

  lcd.clear();

  if (waterDetected == HIGH)
  {
    lcd.setCursor(0,0);
    lcd.print("Water Detected");

    lcd.setCursor(0,1);
    lcd.print("Plant is Wet");

    digitalWrite(buzzerPin, LOW);

    Serial.println("Water Detected");
  }
  else
  {
    lcd.setCursor(0,0);
    lcd.print("No Water!");

    lcd.setCursor(0,1);
    lcd.print("Needs Water");

    digitalWrite(buzzerPin, HIGH);

    Serial.println("No Water Detected");

    delay(300);

    digitalWrite(buzzerPin, LOW);

    delay(300);
  }

  delay(500);
}

```

**Step 3:** Save your code. _See the [Getting Started](../../Getting Started/Arduino_IDE_Setup.md) section_

**Step 4:** Select the arduino board and port _See the [Getting Started](../../Getting Started/Arduino_IDE_Setup.md) section:Selecting Arduino Board Type and Uploading your code_.

**Step 5:** Upload your code. _See the [Getting Started](../../Getting Started/Arduino_IDE_Setup.md) section:Selecting Arduino Board Type and Uploading your code_

## CONCLUSION

The system responds dynamically when physical conditions cross pre-programmed setpoints, driving output devices reliably.
