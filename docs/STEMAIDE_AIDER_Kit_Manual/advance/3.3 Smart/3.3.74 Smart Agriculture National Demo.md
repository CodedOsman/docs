# Project 349
## SMART AGRICULTURE NATIONAL DEMO

**Advanced Embedded Systems Project Using Raspberry Pi Pico 2 W and MicroPython**


## Contents

- [Overview](#overview)
- [Learning Objectives](#learning-objectives)
- [Required Components](#required-components)
- [Before You Begin](#before-you-begin)
- [Circuit Connections](#circuit-connections)
- [Wiring Diagram](#wiring-diagram)
- [Step-by-Step Assembly](#step-by-step-assembly)
- [Testing Individual Components](#testing-individual-components)
- [Full Project Code](#full-project-code)
- [How the Code Works](#how-the-code-works)
- [Expected Result](#expected-result)
- [Troubleshooting](#troubleshooting)
- [Challenge Extensions](#challenge-extensions)
- [Reflection Questions](#reflection-questions)
- [Save Your Work](#save-your-work)
- [Next Project](#next-project)

---

## Overview

The Pico demonstrates how one farm-monitoring prototype could be adapted for different agro-ecological profiles using local mode switching.

National-scale claims are too abstract when students never test how the same prototype would behave under different local farming profiles.

A Pico 2 W prototype with a soil moisture sensor, DHT22, light sensor, mode button, and OLED regional-profile dashboard.

Profile-based adaptation, transparent thresholds, and honest national-demo wording.

### Project Story

**Advanced Project**: This advanced project is designed to help learners move beyond basic wiring and coding into complete system thinking. The learner should build the prototype, test each subsystem, validate the data, explain the design decisions, and propose improvements for real-world deployment.

A prototype that works in one local environment may need different thresholds in another. This build helps students compare how one agriculture node could be adapted for different regions or farming profiles, while staying honest that it is a classroom demo and not a national deployment.

---

## Learning Objectives

- Read and validate live sensor data
- Interpret a threshold or category instead of only reading raw numbers
- Use a local display or serial output as a simple dashboard
- Check library setup and wiring before running the full system
- Discuss calibration limits before real deployment

---

## Required Components



|  |  |  |  |
| --- | --- | --- | --- |
| ![![Capacitive soil moisture sensor](../../../assets/aider/components/Soil_Moisture_Sensor.png)<br>Capacitive soil moisture sensor](../../../assets/aider/components/Soil_Moisture_Sensor.png)<br>![Capacitive soil moisture sensor](../../../assets/aider/components/Soil_Moisture_Sensor.png)<br>Capacitive soil moisture sensor | ![![128x64 I2C OLED display](../../../assets/aider/components/OLED_Displays.png)<br>128x64 I2C OLED display](../../../assets/aider/components/LEDs__single-color.png)<br>![128x64 I2C OLED display](../../../assets/aider/components/OLED_Displays.png)<br>128x64 I2C OLED display | ![](../../../assets/aider/components/RASPBERRY_Pi_PICO_2_W.png)<br> | ![](../../../assets/aider/components/RASPBERRY_Pi_PICO_2_W.png)<br> |



---

## Before You Begin

Before starting this project, make sure you have completed the foundational sections at the beginning of the manual:

- **Software Installation and Setup**
- **Safety Guidelines**
- **Breadboard Basics**
- **Reading Circuit Diagrams**

### Project-Specific Setup Notes

- Upload ssd1306.py to the root folder of the Pico file system in Thonny before running the full project.
- In the Thonny Shell, run `import os` and `print(os.listdir())` to confirm that ssd1306.py is present on the Pico.
- This project is a local adaptation demo only. It does not represent a real national agriculture platform or deployment program.

```python
import os
print(os.listdir())
```

### Project-Specific Safety Note

Use profile names as teaching examples and avoid presenting the thresholds as official agricultural guidance.

---

## Circuit Connections



|  |  |  |  |
| --- | --- | --- | --- |
| ![![Capacitive soil moisture sensor](../../../assets/aider/components/Soil_Moisture_Sensor.png)<br>Capacitive soil moisture sensor](../../../assets/aider/components/Soil_Moisture_Sensor.png)<br>![Capacitive soil moisture sensor](../../../assets/aider/components/Soil_Moisture_Sensor.png)<br>Capacitive soil moisture sensor | ![![128x64 I2C OLED display](../../../assets/aider/components/OLED_Displays.png)<br>128x64 I2C OLED display](../../../assets/aider/components/LEDs__single-color.png)<br>![128x64 I2C OLED display](../../../assets/aider/components/OLED_Displays.png)<br>128x64 I2C OLED display | ![](../../../assets/aider/components/RASPBERRY_Pi_PICO_2_W.png)<br> | ![](../../../assets/aider/components/RASPBERRY_Pi_PICO_2_W.png)<br> |

| --- | --- | --- | --- |
![Raspberry Pi Pico 2 W](../../../assets/aider/components/RASPBERRY_Pi_PICO_2_W.png)<br>Raspberry Pi Pico 2 W | ![Capacitive soil moisture sensor](../../../assets/aider/components/Soil_Moisture_Sensor.png)<br>Capacitive soil moisture sensor | ![DHT22 sensor](../../../assets/aider/components/DHT22_sensor.png)<br>DHT22 sensor | ![LDR with 10k resistor or analog light sensor](../../../assets/aider/components/LDR__light-dependent_resistor.png)<br>LDR with 10k resistor or analog light sensor
![Momentary push button](../../../assets/aider/components/Push_buttons.png)<br>Momentary push button | ![128x64 I2C OLED display](../../../assets/aider/components/OLED_Displays.png)<br>128x64 I2C OLED display | ![](../../../assets/aider/components/RASPBERRY_Pi_PICO_2_W.png)<br> | ![](../../../assets/aider/components/RASPBERRY_Pi_PICO_2_W.png)<br>

|---------------|-------------|---------------------------------|-------|
| Soil sensor AO | GPIO 26 ADC0 | GPIO 26 / physical pin 31 | Soil input |
| Light sensor output | GPIO 27 ADC1 | GPIO 27 / physical pin 32 | Light input |
| DHT22 DATA | GPIO 15 | GPIO 15 / physical pin 20 | Climate input |
| Button leg 1 | GPIO 16 | GPIO 16 / physical pin 21 | Profile button input |
| Button leg 2 | 3.3V output | Physical pin 36 | High when pressed |
| OLED SDA | GPIO 20 | GPIO 20 / physical pin 26 | I2C0 SDA |
| OLED SCL | GPIO 21 | GPIO 21 / physical pin 27 | I2C0 SCL |

---

## Wiring Diagram

```
Soil sensor AO -> GPIO 26
Light sensor output -> GPIO 27
DHT22 DATA -> GPIO 15
Button -> GPIO 16 and 3.3V
GPIO 20 -> OLED SDA
GPIO 21 -> OLED SCL
Common GND -> sensors, button, and OLED
```

Diagram Placeholder
[Insert wiring diagram or breadboard image here]

---

## Step-by-Step Assembly

1. Connect the soil sensor analog output to GPIO 26.
2. Build the light sensor analog path and connect it to GPIO 27.
3. Connect the DHT22 to Pico 3.3V, GND, and GPIO 15.
4. Wire the profile button between GPIO 16 and 3.3V so the input goes high when pressed.
5. Connect the OLED to Pico 3.3V, GND, GPIO 20, and GPIO 21, then upload ssd1306.py to the Pico.

---

## Testing Individual Components

Before running the full project, test each subsystem separately. This makes it easier to find wiring, library, or logic problems before full integration.

1. **Hardware setup**: Assemble the Pico, sensor, indicator, and load wiring exactly as shown in the connection table before applying power.
2. **Test the input sensor**: Test the sensors and the profile button separately so the adaptation demo starts from trusted values.
3. **Test the output device**: Run an OLED hello-world test before adding the profile logic.
4. **Test the decision logic**: Keep the same physical condition and switch profiles to compare how the recommendation changes.
5. **Run the full system**: Run the full demo and compare the printed values with the profile-specific recommendation.
6. **Validate the prototype**: Discuss what extra field data would be needed before adapting one prototype for real use in many regions.
7. **Save the project**: Save the validated program on the Pico as main.py and keep a copy on the computer for future edits.

---

## Full Project Code

After completing and checking the circuit connections, open Thonny IDE, copy and paste this code into a new file or upload the project file to the Raspberry Pi Pico 2 W, then run it from Thonny.

Upload and run this code after the individual tests work correctly.

```python
from machine import ADC, I2C, Pin
import dht
import time

try:
    from ssd1306 import SSD1306_I2C
    HAS_OLED = True
except ImportError:
    HAS_OLED = False

SOIL_PIN = 26
LIGHT_PIN = 27
DHT_PIN = 15
BUTTON_PIN = 16
I2C_SDA_PIN = 20
I2C_SCL_PIN = 21
SOIL_DRY_RAW = 48000
SOIL_WET_RAW = 18000
PROFILES = ['COASTAL', 'FOREST', 'SAVANNA']

soil = ADC(SOIL_PIN)
light = ADC(LIGHT_PIN)
climate = dht.DHT22(Pin(DHT_PIN))
button = Pin(BUTTON_PIN, Pin.IN, Pin.PULL_DOWN)
i2c = I2C(0, sda=Pin(I2C_SDA_PIN), scl=Pin(I2C_SCL_PIN), freq=400000)
if HAS_OLED:
    oled = SSD1306_I2C(128, 64, i2c)

profile_index = 0
last_button = 0


def clamp(value, low, high):
    return max(low, min(high, value))


def dryness_percent(raw):
    span = SOIL_DRY_RAW - SOIL_WET_RAW
    if span <= 0:
        return 0
    pct = int((raw - SOIL_WET_RAW) * 100 / span)
    return clamp(pct, 0, 100)


print('Smart agriculture national demo ready')

while True:
    button_now = button.value()
    if button_now == 1 and last_button == 0:
        profile_index = (profile_index + 1) % len(PROFILES)
    last_button = button_now

    dry_pct = dryness_percent(soil.read_u16())
    light_raw = light.read_u16()
    try:
        climate.measure()
        temperature = climate.temperature()
    except OSError:
        temperature = 0

    profile = PROFILES[profile_index]
    if profile == 'COASTAL':
        recommend = 'IRRIGATE' if dry_pct >= 60 else 'WAIT'
    elif profile == 'FOREST':
        recommend = 'IRRIGATE' if dry_pct >= 70 else 'WAIT'
    else:
        recommend = 'IRRIGATE' if dry_pct >= 55 or temperature >= 32 or light_raw > 45000 else 'WAIT'

    print('Profile:{} Dry:{}% Temp:{}C Light:{} Recommend:{}'.format(profile, dry_pct, temperature, light_raw, recommend))

    if HAS_OLED:
        oled.fill(0)
        oled.text('National Demo', 0, 0)
        oled.text(profile[:16], 0, 18)
        oled.text('D:{} T:{}'.format(dry_pct, temperature), 0, 34)
        oled.text(recommend[:16], 0, 50)
        oled.show()

    time.sleep(2)
```

---

## How the Code Works

| Code Section | What It Does | Why It Matters | What to Modify During Testing |
|--------------|--------------|----------------|------------------------------|
| Profile adaptation | Applies different threshold sets to the same local measurements. | This demonstrates how one prototype might be adapted across regions without claiming a real deployment. | Change one profile at a time so the differences remain easy to explain. |
| Button profile switching | Cycles through the regional profiles with one button. | The user can compare adaptation choices quickly on the same hardware. | Use deliberate presses and keep edge detection in place. |
| OLED demo view | Shows the active profile and resulting recommendation. | The display keeps the adaptation logic visible during testing. | Verify ssd1306.py and the I2C wiring if the display is blank. |

---

## Expected Result

The serial monitor reports the current reading or state clearly.

The hardware output responds when the decision logic changes state.

Subsystem behavior matches the thresholds, timing, or rules described in the document.

### Validation Checks

- **Normal condition test**: confirm the system stays in its safe or idle state under baseline conditions.
- **Warning condition test**: move the input close to the chosen threshold and confirm the transition is sensible.
- **Critical condition test**: trigger the strongest alert or control state and confirm the output response is correct.
- **Calibration test**: compare the sensor or timing against a known reference or repeated trial.
- **Limitation test**: deliberately create an awkward or noisy condition and note how the prototype behaves.

### Deployment and Limitations

- This prototype works well for classroom demonstrations and local monitoring pilots.
- Before deployment, it needs calibration records, protective mounting, and regular validation checks.
- Display-only systems still depend on correct sensor placement and trained users.

---

## Troubleshooting

| Problem | Possible Cause | Solution |
|---------|----------------|----------|
| The profile changes too fast | The button is bouncing or being held too long | Use deliberate presses and verify the edge-based button logic. |
| The recommendation never changes | The thresholds do not match your test conditions | Create clearer soil or temperature changes and adjust one profile at a time. |
| Light values seem unrealistic | The light-sensor divider is incorrect | Print the raw ADC value and rebuild the analog light path if needed. |
| OLED is blank | The display library or I2C wiring is wrong | Confirm ssd1306.py is present and verify GPIO 20 and GPIO 21. |

---



## Save Your Work

Save the file to your computer as:

```
project_349_smart_agriculture_national_demo.py
```

If you want the program to run automatically when the Pico powers on, save the final version to the Pico as:

```
main.py
```

---

